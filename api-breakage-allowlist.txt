# scripts/check-api.sh が互換破壊として扱わない行。
#
# 意図して公開 API を壊すとき (メジャーバージョンを上げるとき) に、check-api.sh が
# 出力した行 (例: "Var PC88.frameRate has been removed") をそのまま 1 行ずつ書く。
# 行全体が一致したものだけを除く。# で始まる行と空行は読まない。
#
# メジャーのタグを打つと比較の基準がそのタグに進むので、そのとき書いた行は消す。
